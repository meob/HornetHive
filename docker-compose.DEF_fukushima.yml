services:
  mqtt-broker:
    image: eclipse-mosquitto:latest
    ports: ["1883:1883", "9001:9001"]
    volumes: ["./mosquitto.conf:/mosquitto/config/mosquitto.conf"]

  gcs-backend:
    build: { context: ., dockerfile: Dockerfile.node }
    ports: ["3000:3000"]
    command: ["node", "server.js", "--scenario", "FUKUSHIMA_PLANT", "--debug"]
    environment: ["MQTT_HOST=mqtt-broker"]
    depends_on: ["mqtt-broker"]

  radar-plant:
    build: { context: ., dockerfile: Dockerfile.python-light }
    environment: ["PYTHONUNBUFFERED=1"]
    command: ["python", "assets/radar_mock.py", "--id", "PLANT", "--range","2000", "--speed","30", "--mqtt-host", "mqtt-broker"]
    depends_on: ["mqtt-broker"]

  drone-thermal1:
    build: { context: ., dockerfile: Dockerfile.python-light }
    command: ["python", "assets/drone_mock.py", "SCOUT_01", "MINI_4", "--mqtt-host", "mqtt-broker"]
    depends_on: ["mqtt-broker"]

  drone-thermal2:
    build: { context: ., dockerfile: Dockerfile.python-light }
    command: ["python", "assets/drone_mock.py", "SCOUT_02", "MINI_4", "--mqtt-host", "mqtt-broker"]
    depends_on: ["drone-thermal1"]

  drone-thermal3:
    build: { context: ., dockerfile: Dockerfile.python-light }
    command: ["python", "assets/drone_mock.py", "SCOUT_03", "MINI_4", "--mqtt-host", "mqtt-broker"]
    depends_on: ["drone-thermal2"]

  drone-thermal4:
    build: { context: ., dockerfile: Dockerfile.python-light }
    command: ["python", "assets/drone_mock.py", "SCOUT_04", "MINI_4", "--mqtt-host", "mqtt-broker"]
    depends_on: ["drone-thermal3"]

  drone-med1:
    build: { context: ., dockerfile: Dockerfile.python-light }
    command: ["python", "assets/drone_mock.py", "SCOUT_05", "MATRICE_350_RTK", "--mqtt-host", "mqtt-broker"]
    depends_on: ["drone-thermal4"]

  drone-med2:
    build: { context: ., dockerfile: Dockerfile.python-light }
    command: ["python", "assets/drone_mock.py", "SCOUT_06", "MATRICE_350_RTK", "--mqtt-host", "mqtt-broker"]
    depends_on: ["drone-med1"]

  target-leak:
    build: { context: ., dockerfile: Dockerfile.python-light }
    command: ["python", "assets/target_mock.py", "LEAK on 5", "--type","☢︎", "--action","SENSOR", "--delay","10", "--pos","1110,900", "--area","1500", "--drift","0.0", "--mqtt-host","mqtt-broker"]
    depends_on: ["mqtt-broker"] 

  target-intruder1:
    build: { context: ., dockerfile: Dockerfile.python-light }
    command: ["python", "assets/target_mock.py", "INTRUDER", "--type","🪖", "--action","SENSOR", "--delay","60", "--offset","1200,300", "--area","500", "--radius","30", "--drift","1.5", "--mqtt-host","mqtt-broker"]
    depends_on: ["mqtt-broker"]

  target-worker:
    build: { context: ., dockerfile: Dockerfile.python-light }
    command: ["python", "assets/target_mock.py", "Worker", "--type","👷‍♀️", "--action","MEDKIT", "--delay","120", "--offset","600,100", "--area","400", "--radius","30", "--drift","0.5", "--mqtt-host","mqtt-broker"]
    depends_on: ["mqtt-broker"]